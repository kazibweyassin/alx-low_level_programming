#include "main.h"
#include "stdio.h"
/**
 * main - prints the alphabet
 * Return: Always(Success)
 */

int main(void)
{
	char c;

	for (c = 'a'; c <= 'z' ; c++)
	{
	_putchar(c);
	}
	putchar('\n');
return (0);
}
